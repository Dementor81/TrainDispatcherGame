Features:
    
    ☐ total delays and train misshapens display for the gamemaster
    ☐ add a signal for the wrong line
    ☐ add a Ignore next signal button to the UI that let trains go over the signal
    ☐ improve how all games are handled, add a simple way to quit a game
    ☐ reduce the amount of requests to the server (upcoming trains and status are requested too often)
    ☐ check all client side events and if they are still used and needed
    ✔ MultipleUnit trains for s_bahn and ice trains @done(26-04-08 19:43)
    ✔ if a train becomes another train, display the new train number in the UI @done(26-03-29 13:07)
    ✔ touchsupport @done(26-03-28 19:04)
    ✔ adding a key to start a new game @done(26-03-22 09:13)
    ✔ improve the networking code to harden it against short disconnects @done(26-03-21 12:55)
    ✔ handle missrouted trains on client side. maybe it should stop at the exitppoint and the player must confirm  backing up to the station @done(26-03-05 12:17)
    ✔ select a train to see different information @done(26-03-03 07:40)

Big Features:    
    
    ☐ coupling and decoupling
    ☐ slow down trains if the train is on a router with switches set to the branch
    ☐ add a admin page that allows to see all games and players
    ☐ settings page on game master panel for settlings like "show train labels" or random delays
    ✔ acceleration and deceleration @done(26-03-03 07:40)
    ✔ shunting @done(26-03-03 07:40)
    ✔ add game logic to the server to handle multiple game sessions simultaniously @done(26-02-22 18:22)
    ✔ better UI for station selection with a display which station is already taken by whom @done(26-02-24 19:22)

Bugs:

    ☐ some random delays of trains at the server side
    ☐ the server dispatch does not calculate travel times within stations, from spawnpoint to stoppingpoint
    ☐ stoping trains might be shown with same arrival and departure time, if its the same minute
    ☐ train delays are not shown currectly
    ✔ ending trains show some bugs @done(26-04-14 20:33)
    ✔ fix UI on tablet @done(26-03-28 19:04)
    ✔ traindetails panel does stay open when the train is exited @done(26-03-21 12:50)
    














'It adds 6 shortcuts when editing a Todo file:'

'Cmd/Ctrl+Enter' // Triggers `Todo: Toggle Box`
'Alt+Enter' // Triggers `Todo: Toggle Box`
'Alt+D' // Triggers `Todo: Toggle Done`
'Alt+C' // Triggers `Todo: Toggle Cancelled`
'Alt+S' // Triggers `Todo: Toggle Start`
'Cmd/Ctrl+Shift+A' // Triggers  `Todo: Archive`
Archive: