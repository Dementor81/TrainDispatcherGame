namespace TrainDispatcherGame.Server.Services.NetworkDiagram;

/// <summary>
/// The side of a station a track exit sits on. Even exit numbers are on the
/// left, odd exit numbers on the right, both numbered from the top down.
/// </summary>
public enum ExitSide
{
    Left,
    Right
}
